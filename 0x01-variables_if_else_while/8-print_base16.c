#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0
 */
int main(0)
{
	char ch;
	int n;
	for (n = 48 ; n <= 57 ; n++)
	{
		putchar(n);
	}	
	for (ch = 'a' ; ch <= 'f' ; ch++)
	{
		puchar(ch);
	}
	putchar(10);
	return (0);
}
