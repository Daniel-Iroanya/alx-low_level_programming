#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 always
 */
int main(void)
{
	int a;

	for (a = 97 ; a <= 122 ; a++);
	{
	putchar(a);
	}
	for (a = 65 ; A <= 90 ; A++);
	{
	putchar(a);
	}
	putchar(10);
	return (0);
}
