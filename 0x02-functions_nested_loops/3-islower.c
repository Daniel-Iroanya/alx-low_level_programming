#include "main.h"

/**
 * _islower -> checks if the character is lower case
 *
 * @c: character argument
 * Return: return 1 and 0 based on the argument
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
